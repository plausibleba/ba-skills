# Paired Negative Fixture Suite

Each case folder contains a **scaffold.json** and **heatmap.json** intended to be loaded together.
All cases should be schema-valid but fail **semantic** validation or cross-file integrity rules.

See manifest.csv for the intended failure for each case.
