# Cover Notes — for sending to each reviewer

One-paragraph intro to paste alongside the briefing + role prompt + spike artefact when you send each reviewer their materials. These are written in your voice; tweak as you like.

---

## Cover Note for Reviewer 1 — Graph-DB / Runtime Veteran (GPT-5 / OpenAI frontier)

> I'd like your read on a Phase-1 design-spar for the Value Cognition Canvas project. We're committing to a graph runtime as the canonical data layer — the current flat-JSON scaffold has hit its expressiveness ceiling — and the proposal is to use embedded RDF/SHACL/JSON-LD via `oxigraph-wasm`, with the JSON-LD bundle becoming our published open-core contract. I've attached three things: a shared briefing (`BRIEFING.md`), your role prompt with the specific challenges I want pressure-tested (`REVIEWER-1-graph-veteran.md`), and a working green spike (`spike/graph-runtime/`) that demonstrates the runtime against four success criteria including a bug-injection test. Your priors from shipping graph databases at scale are exactly the diversity of judgement I'm looking for — I want to know where the proposal hides its operational costs, whether a property-graph alternative could have got 80% of the value at lower runtime cost, and whether the three constraints I've placed on the runtime hold under real load. The role prompt sets out the specific output format and a 60–90 min reading + 30–45 min writing time-box; this is a challenge, not an exhaustive review. The architect's position is locked on a few things (CAPSICUM as the metamodel target, graph-as-canonical, solo-dev operability — all detailed in §6 of the briefing) but the runtime choice itself is genuinely up for grabs. Give me your strongest objections.

---

## Cover Note for Reviewer 2 — Standards / Ontology Sceptic (Gemini)

> I'd like your read on a Phase-1 design-spar for the Value Cognition Canvas project. The architect's position commits CAPSICUM — a 15-year-old ontology of purposeful action under authority, recently formalised in an arXiv paper *A Logical Model of Endeavour* — to a published RDF/SHACL/JSON-LD shape that becomes the open-core contract of an enterprise governance platform. I've attached four things: a shared briefing (`BRIEFING.md`), your role prompt with specific challenges (`REVIEWER-2-standards-sceptic.md`), a working spike that demonstrates the runtime end-to-end (`spike/graph-runtime/`), and the foundational sources for the ontology (the arXiv paper and the framework reference document). Your priors from working with formal ontologies, W3C and OMG specs, and SHACL/JSON-LD in production are exactly the diversity I need — I want to know whether the spike's six-class CAPSICUM shape is a credible v0 of the contract, whether SHACL actually covers what CAPSICUM specifies for *Terms* (the Information × Governance cell), and whether the JSON-LD `@context` is durable enough to publish without facing a breaking v1 inside twelve months. There's also a downstream BACM v1.0 round-trip claim I'd like pressure-tested. The role prompt sets out the output format and a 60–90 min reading + 30–45 min writing time-box. The runtime choice is Reviewer 1's domain; the LLM pipeline is Reviewer 3's; your remit is the formal ontology and its standards-conformant expression. Be the reviewer who knows where the wallpaper hides cracks.

---

## Cover Note for Reviewer 3 — LLM Pipeline Architect (fresh Claude session)

> Open this as a fresh Cowork session against the Value Cognition Canvas working folder — I want this review independent of the architectural conversation that produced the proposal you're being asked to challenge. The architect's position commits VCC to an embedded RDF/SHACL/JSON-LD runtime as the canonical data layer, and crucially, adopts an **externalised-Outcome** metamodel — a structural shift from the current internalised-lifecycle pattern that the multi-pass LLM pipeline currently produces. I've attached the shared briefing (`spike/graph-runtime/spar/BRIEFING.md`) and your role prompt (`REVIEWER-3-pipeline-architect.md`); both reference specific pipeline files in the repo (`packages/frontend/src/domain/pipeline/`) that you'll need to read to give a real review. Your remit is whether the pipeline can actually produce well-formed CAPSICUM-shaped output reliably from real transcripts — particularly externalised Outcomes with `stateOf` and `triggers`, the AgentCharter shape that the OSS strategy names as a proprietary reference plugin, and the SHACL-style validation feeding back into the bounded-repair loops. The runtime choice and ontology shape are being challenged by other reviewers; your domain is the pipeline. Time-box: 60–90 min reading + 30–45 min writing, with the output structure given in the role prompt. The architect has been clear that "graph is canonical, scaffold serialises it" is settled — but a metamodel commitment that the LLM pipeline can't produce reliably is no commitment at all. Read enough of the prompts and the gates code to ground your concerns in what's actually implemented today, and tell me what breaks.

---

## Cover Note for Reviewer 4 — Commercial / OSS-Strategy Sceptic (fresh GPT-5 session, separate from Reviewer 1)

> I'd like a Phase-1 design-spar review focused specifically on the commercial and open-source strategy implications of a technical decision the Value Cognition Canvas project is about to lock in. The architect is committing to an embedded RDF/SHACL/JSON-LD runtime — and crucially, the JSON-LD bundle becomes the published Layer-1 contract of an open-source platform governed by a new PlausibleBA Foundation, with a four-layer stack (open core / association content / hosted platform / commercial plugins) and the Apptio/TBM-Council acquisition as the explicit reference case. I've attached four things: a shared briefing (`BRIEFING.md`), your role prompt with the specific challenges I want pressure-tested (`REVIEWER-4-oss-strategy-sceptic.md`), the OSS strategy doc (`PlausibleBA_Open_Source_Strategy v2.0.docx`), and the agentic-enablement PRD that frames the proprietary plugin moat (`PRD - Agentic_Enterprise_Framework v0.1.docx`). Three other reviewers are challenging the technical aspects (runtime, ontology, LLM pipeline); your remit is everything *else* — does this technical commitment strengthen or weaken the platform strategy, the partner ecosystem story, the Anthropic Partner Network pitch, the multi-stakeholder governance ergonomics, and the acquisition-protection posture. I'm specifically interested in your priors on platform strategy, OSS business models, and partner ecosystem design — not on the technology itself. Time-box: 60–90 min reading + 30–45 min writing, output structure in the role prompt. The strategy doc has been written with confidence; I'd like you to bring scepticism. Even if the architecture is correct, does it earn its keep against the platform strategy that justifies building it?
